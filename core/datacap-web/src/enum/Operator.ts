export enum Operator
{
  EQ = ("="),
  NEQ = ("<>"),
  GT = (">"),
  GTE = (">="),
  LT = ("<"),
  LTE = ("<="),
  LIKE = ("LIKE"),
  NLIKE = ("NOT LIKE"),
  NULL = ("IS NULL"),
  NNULL = ("IS NOT NULL")
}
