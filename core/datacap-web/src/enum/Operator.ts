export enum Operator
{
  EQ = ("="),
  NEQ = ("<>"),
  GT = (">"),
  GTE = (">="),
  LT = ("<"),
  LTE = ("<="),
  LIKE = ("LIKE"),
  NLINK = ("NOT LIKE"),
  NULL = ("IS NULL"),
  NNULL = ("IS NOT NULL")
}
