export enum Type
{
  DIMENSION = ('DIMENSION'),
  METRIC = ('METRIC'),
  GROUP = ('GROUP'),
  FILTER = ('FILTER'),
}

export enum ColumnType
{
  STRING = 'STRING',
  NUMBER = 'NUMBER',
  BOOLEAN = 'BOOLEAN'
}
